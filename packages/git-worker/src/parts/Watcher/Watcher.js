const setupWatcher = () => {
  // const root = vscode.workspace // TODO don't couple vscode api with business logic
  // if (!root) {
  //   return
  // }
  // const rootWatcher = FileSystem.watch(root, {
  //   exclude: ['node_modules', '.git', 'dist'],
  // })
  // const dotGitWatcher = FileSystem.watch(
  //   `${root}/.git/refs/remotes/origin/master`, // TODO get dynamic branch name
  //   {
  //     exclude: [],
  //   }
  // )
  // rootWatcher.on('all', (event) => {
  //   // vscode.updateGitDecorations()
  // })
  // dotGitWatcher.on('all', (event) => {
  //   // vscode.updateGitDecorations()
  // })
}
